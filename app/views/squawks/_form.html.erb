<%= bootstrap_form_for(@squawk) do |f| %>

  <div class="field">
    <%= f.text_area :body %>
  </div>

  <div class="field">
    <%= f.hidden_field :user_id, value: current_user.id %>
  </div>

  <div class="actions">
    <%= f.submit "Make Squawk" %>
  </div>

<% end %>
